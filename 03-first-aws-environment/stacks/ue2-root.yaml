import:
  - catalog/globals
vars:
  region: us-east-2
  environment: ue2
components:
  terraform:
    tfstate-backend:
      vars:
        name: tfstate
        attributes: [TODO]
        force_destroy: false
        prevent_unencrypted_uploads: true
        enable_server_side_encryption: true
