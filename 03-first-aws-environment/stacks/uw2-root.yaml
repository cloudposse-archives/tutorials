import:
  - catalog/globals
  - catalog/uw2-globals
vars:
  region: us-west-2
  environment: uw2
components:
  terraform:
    tfstate-backend:
      vars:
        name: tfstate-TODO
        force_destroy: false
        prevent_unencrypted_uploads: true
        enable_server_side_encryption: true
